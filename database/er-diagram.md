# ER Diagram — Cafeteria Restaurant Management System

See `backend/prisma/schema.prisma` for the authoritative, enforced schema.
This file mirrors it in diagram form for quick reference — see Phase 5 documentation for the full entity-relationship breakdown and design rationale.

Core entities: User, Category, MenuItem, Ingredient, Inventory, Recipe, Order, OrderItem, Payment, StaffShift, Notification, AuditLog.